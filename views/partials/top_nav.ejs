<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>

    <nav class="mr-8">
      <% if(current_user.isAuthenticated == true) { %>
      <div class="navbar bg-base-100">
        <div class="flex-1">
          <a class="btn btn-ghost normal-case text-xl">Sign in as: <%= current_user.role %></a>
        </div>
        <div class="flex-none">
          <div class="dropdown dropdown-end">
            <div class="flex gap-4 items-center">
              <label tabindex="0" class="btn btn-ghost btn-circle avatar">
                <div class="w-10 rounded-full">
                  <img src="<%= current_user.profile_img %>" />
                </div>
              </label>
              <p><%= current_user.first_nm %></p>
            </div>
            <ul tabindex="0" class="menu menu-compact dropdown-content mt-3 p-2 shadow bg-base-100 rounded-box">
              <li><a href="/auth/logout">Logout <i class="fa-solid fa-arrow-right-from-bracket"></i></a></li>
            </ul>
          </div>
        </div>
      </div>
      <% } %>
    </nav>





    
</body>
</html>